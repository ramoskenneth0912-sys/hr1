# HR1 React SPA — Node/Express API base URL (Step 2/3 foundation).
# Points the frontend at the new Node.js + Express + Auth.js backend.
# Copy this file to `.env` (git-ignored) for local development.
VITE_API_BASE_URL=http://localhost:4000/api/v1
